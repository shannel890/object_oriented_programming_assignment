# Object-Oriented Programming Assignment — Submission README
# ADMISSION == BCS-05-0018/2025
# NAME = SHANNEL KIRUI
This repository contains the C++ assignment for submission and grading. Your instructor/TA will grade the work on GitHub after you push your changes to the repository.

**Repository Overview**
- **Purpose**: C++ assignment for lecture grading.
- **What to submit**: All source files (.cpp/.h), any test files, and this `readme.md`.

**Repository Structure**
- `test/`: (optional) helper or test resources.
- `test1.cpp`: Example or required source file.
- `test2/`: (optional) another folder for project files.
- `test2.cpp`: Another source file.

**Build & Run (local)**
- Use the provided VS Code task: "C/C++: gcc build active file" (default build task).
- Example build command (compile a single file):

```bash
g++ -std=c++17 -g test1.cpp -o test1
```

- Run the binary:

```bash
./test1
```

**Submission (push to GitHub)**
- Commit your work and push to the repository. The grader will check the repository on push.

```bash
git add .
git commit -m "Submit assignment"
git push origin main
```

**Grading Checklist**
- Ensure code compiles without errors.
- Program runs and produces expected output.
- Include any required classes, methods, and interfaces as specified in the assignment.
- Provide brief comments where useful and meaningful variable names.
- This `readme.md` is present and explains how to build and run.

**Notes / Contact**
- If you have questions about the assignment or submission, contact your instructor/TA per course instructions.

Good luck — push when ready!

